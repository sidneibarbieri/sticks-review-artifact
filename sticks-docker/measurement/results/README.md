# Procedural Measurement Results

Keep only reviewer-relevant generated outputs here.

Preferred outputs:

- generated values or intermediate macro inputs
- provenance summaries
- measurement JSON used to justify the study claims
- Docker execution audits and curated Caldera run summaries
- temporary Docker runtime context preparation audits
- consolidated Docker findings reports for framing and discussion

Do not store runtime scratch, caches, or ad hoc debugging files here. If a
temporary directory becomes necessary, create it under a clearly named scratch
path and keep it out of commits.
